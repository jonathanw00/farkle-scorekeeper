# Farkle Scorekeeper

A Farkle scoring app built with React, Tailwind CSS, and Vite.

## Local development

npm install
npm run dev

## Build for production

npm run build

Output goes to `dist/`.

## Deploy

Deployed via Cloudflare Pages, connected to this GitHub repo.
Build command: `npm run build`
Build output directory: `dist`
